<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".app"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomneyApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".login_registro.Activity2_registro_config_hogar"
            android:exported="false" />
        <activity
            android:name=".login_registro.Activity2b_tareas_hogar"
            android:exported="false" />
        <activity
            android:name=".login_registro.Activity3_gastos_recurrentes"
            android:exported="false" />
        <activity
            android:name=".login_registro.Activity4_resumen_registro"
            android:exported="false" />
        <activity
            android:name=".loginActivity"
            android:exported="true"
            android:theme="@style/Theme.HomneyApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HomneyApp.NoActionBar" />

        <!--
            FileProvider para compartir URIs de fichero con la app de cámara.
            Necesario en Android 7+ para evitar FileUriExposedException y para
            que las cámaras OEM (Samsung, Xiaomi…) acepten el destino de la foto.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>