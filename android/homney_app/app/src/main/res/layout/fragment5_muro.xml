<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.fragmento3_tareas.Fragmento3_tareas"
    android:orientation="vertical"
    android:background="@drawable/bg_card_surface2"
    android:layout_margin="@dimen/activity_vertical_margin"
    >

    <!-- Placeholder visible mientras carga o si no hay publicaciones -->
    <TextView
        android:id="@+id/tv_sin_publicaciones"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="32dp"
        android:text="Cargando publicaciones…"
        android:visibility="visible"
        style="@style/Homney.Text.Muted" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="8dp"
        android:clipToPadding="false" />

</LinearLayout>