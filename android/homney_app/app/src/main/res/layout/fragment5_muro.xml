<?xml version="1.0" encoding="utf-8"?>
<!--
    Fragmento 5 — Muro del hogar
    FrameLayout raíz: sin márgenes laterales, fondo consistente con el resto de fragments.
    El RecyclerView maneja su propio scroll — NO se anida dentro de un ScrollView.
    El estado vacío/cargando se superpone centrado gracias al FrameLayout.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- Lista de publicaciones — visible cuando hay datos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="6dp"
        android:paddingBottom="32dp"
        android:clipToPadding="false"
        android:visibility="gone" />

    <!-- Estado vacío / cargando — centrado vertical y horizontalmente -->
    <TextView
        android:id="@+id/tv_sin_publicaciones"
        style="@style/Homney.Text.Muted"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="32dp"
        android:text="@string/cargando_publicaciones"
        android:visibility="visible" />

</FrameLayout>
